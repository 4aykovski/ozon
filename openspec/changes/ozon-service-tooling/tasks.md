# Задачи

- [x] Скопировать `.golangci.yml` ядра и убрать core-специфичные исключения
  путей.
- [x] Добавить `Taskfile.yml` с задачами `build`, `run`, `lint`, `test`,
  `generate`.
- [x] Убедиться, что `task lint` проходит на текущем каркасе.
- [x] Запустить `task build`, `task test` и убедиться, что команды работают.
- [x] Отметить выполненные пункты чеклиста после проверки.
